# 一日三餐

基于 VitePress 的个人菜谱与厨房记录站点。

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
npm run preview
```

推送到 `main` 分支后，GitHub Actions 会自动构建并发布到 GitHub Pages。首次发布前，请在仓库设置中将 Pages 的部署来源设置为 `GitHub Actions`。
